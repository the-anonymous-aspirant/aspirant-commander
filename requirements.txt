fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.3
dateparser==1.2.0
pytest==8.3.4
httpx==0.28.1
# Valuation Statement tool (Värdeutlåtande):
pdfplumber==0.11.4
pymupdf==1.24.13
python-docx==1.1.2
python-multipart==0.0.20
# OCR fallback for image-only PDFs (#5907). pytesseract drives the tesseract-ocr
# binary installed in Dockerfile-Commander; Pillow hands it the rasterised page.
# Pillow is already pulled transitively by pdfplumber — pinned here because the
# OCR module imports it directly, so the dependency should be declared, not
# incidental. Raster is via pymupdf (above); no poppler/pdf2image is needed.
pytesseract==0.3.13
Pillow==12.3.0
